import React from 'react';
import Nav from '../components/Nav';
import DogForm from '../components/DogForm';

const MainDogForm = (props) => {
    return(
        <div>
            <Nav />
            <DogForm />
        </div>
    )
}

export default MainDogForm;